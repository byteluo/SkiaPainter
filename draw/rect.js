const canvas = new Canvas(500, 500);
canvas.fillRect(100, 100, 100, 100);
canvas.strokeRect(0, 0, 100, 100);
canvas.stroke();
canvas.arc(150, 150, 50, 0.2 * Math.PI);
canvas.arc()
canvas.save("d:/draw/rect.png");